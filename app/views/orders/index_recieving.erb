<% provide(:title, "受注一覧") %>
<h1>受注一覧</h1>
<div>
  <%= @orders.count %>件
  <ol>
    <% @orders.each do |order| %>
      <%= render partial: 'orders/order', locals: { order: order} %>
    <% end %>
  </ol>
</div>