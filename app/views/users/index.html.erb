<% provide(:title, 'ユーザー一覧') %>
<div class="container-fluid">
  <h3 class="my-3">ユーザー一覧</h3>
  <ul class="list-unstyled">
    <% @users.each do |user| %>
      <li class="my-2 list-unstyled">
        <%= render user %>
      </li>
    <% end %>
  </ul>
  <%= paginate @users %>
</div>